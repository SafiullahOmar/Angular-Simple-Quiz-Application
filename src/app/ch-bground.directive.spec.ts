import { ChBgroundDirective } from './ch-bground.directive';

describe('ChBgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ChBgroundDirective();
    expect(directive).toBeTruthy();
  });
});
